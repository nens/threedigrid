# Config file for automatic testing at travis-ci.org
sudo: required

language: python
python:
  - 2.7

services:
  - docker

before_install:
  - docker build -t threedigrid/test .

script:
  - docker run --rm carlad/sinatra pytest --cov=threedigrid